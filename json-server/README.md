# JSON Server API Documentation

Документация по API для приложения изучения языков.

## 🚀 Запуск сервера

### Способ 1: Через npm скрипт
```bash
npm run start:dev:server
```

### Способ 2: Напрямую через Node.js
```bash
node ./json-server/index.js
```

Сервер запустится на порту **8000** и будет доступен по адресу: `http://localhost:8000`

---

## 📋 Структура данных

### Профиль (Profile)
```json
{
  "firstName": "Андрей",
  "lastName": "Мушовец",
  "age": 22
}
```

### Слово (Word)
```json
{
  "id": "1",
  "russian": "привет",
  "english": "hello",
  "isGuessed": false
}
```

---

## 🔌 Эндпоинты API

### 1. Получить случайное слово

Получает случайное слово из тех, которые еще не были угаданы (`isGuessed: false`).

**Endpoint:** `GET /words/random`

**Request:**
```bash
curl http://localhost:8000/words/random
```

**Response 200 OK:**
```json
{
  "id": "1",
  "russian": "привет",
  "english": "hello",
  "isGuessed": false
}
```

**Response 404 Not Found:**
```json
{
  "message": "No unguessed words available"
}
```

---

### 2. Проверить ответ и обновить статус

Проверяет правильность ответа пользователя. Если ответ правильный, устанавливает `isGuessed: true` для слова.

**Endpoint:** `POST /words/check`

**Request Body:**
```json
{
  "wordId": "1",
  "english": "hello"
}
```

**Request:**
```bash
curl -X POST http://localhost:8000/words/check \
  -H "Content-Type: application/json" \
  -d '{"wordId": "1", "english": "hello"}'
```

**Response 200 OK (Правильный ответ, слово еще не было угадано):**
```json
{
  "success": true,
  "message": "Correct answer!",
  "word": {
    "id": "1",
    "russian": "привет",
    "english": "hello",
    "isGuessed": true
  }
}
```

**Response 200 OK (Правильный ответ, но слово уже было угадано):**
```json
{
  "success": true,
  "message": "Correct answer, but word was already guessed",
  "word": {
    "id": "1",
    "russian": "привет",
    "english": "hello",
    "isGuessed": true
  }
}
```

**Response 200 OK (Неправильный ответ):**
```json
{
  "success": false,
  "message": "Incorrect answer",
  "word": {
    "id": "2",
    "russian": "дом",
    "english": "house",
    "isGuessed": false
  }
}
```

**Response 400 Bad Request:**
```json
{
  "message": "wordId and english are required"
}
```

**Response 404 Not Found:**
```json
{
  "message": "Word not found"
}
```

**Примечания:**
- Проверка ответа происходит без учета регистра
- Лишние пробелы в начале и конце автоматически удаляются
- Если ответ правильный и слово еще не было угадано, `isGuessed` автоматически устанавливается в `true`

---

### 3. Сбросить все результаты

Устанавливает `isGuessed: false` для всех слов в базе данных.

**Endpoint:** `POST /words/reset`

**Request:**
```bash
curl -X POST http://localhost:8000/words/reset
```

**Response 200 OK:**
```json
{
  "success": true,
  "message": "All words reset successfully",
  "resetCount": 10000
}
```

---

### 4. Получить все угаданные слова

Возвращает массив всех слов, у которых `isGuessed: true`. Используется для отображения прогресса в профиле.

**Endpoint:** `GET /words/guessed`

**Request:**
```bash
curl http://localhost:8000/words/guessed
```

**Response 200 OK:**
```json
[
  {
    "id": "1",
    "russian": "привет",
    "english": "hello",
    "isGuessed": true
  },
  {
    "id": "3",
    "russian": "кот",
    "english": "cat",
    "isGuessed": true
  }
]
```

**Response 200 OK (Пустой массив, если нет угаданных слов):**
```json
[]
```

---

### 5. Получить профиль

Получает данные текущего профиля пользователя.

**Endpoint:** `GET /profile`

**Request:**
```bash
curl http://localhost:8000/profile
```

**Response 200 OK:**
```json
{
  "firstName": "Андрей",
  "lastName": "Мушовец",
  "age": 22
}
```

---

### 6. Обновить профиль

Обновляет данные профиля. Можно обновить одно или несколько полей одновременно.

**Endpoint:** `PATCH /profile`

**Request Body:**
```json
{
  "firstName": "Иван",
  "lastName": "Петров",
  "age": 25
}
```

Все поля опциональны. Обновляются только переданные поля.

**Request:**
```bash
curl -X PATCH http://localhost:8000/profile \
  -H "Content-Type: application/json" \
  -d '{"firstName": "Иван", "age": 25}'
```

**Response 200 OK:**
```json
{
  "firstName": "Иван",
  "lastName": "Мушовец",
  "age": 25
}
```

**Response 404 Not Found:**
```json
{
  "message": "Profile not found"
}
```

**Примеры:**

Обновить только имя:
```bash
curl -X PATCH http://localhost:8000/profile \
  -H "Content-Type: application/json" \
  -d '{"firstName": "Петр"}'
```

Обновить только возраст:
```bash
curl -X PATCH http://localhost:8000/profile \
  -H "Content-Type: application/json" \
  -d '{"age": 30}'
```

---

## 📝 Стандартные эндпоинты JSON Server

Помимо кастомных эндпоинтов, доступны стандартные эндпоинты JSON Server:

- `GET /words` - получить все слова
- `GET /words/:id` - получить слово по ID
- `GET /profile` - получить профиль (также доступен кастомный эндпоинт)

---

## ⚠️ Ошибки

Все эндпоинты возвращают стандартные HTTP статус-коды:

- `200` - Успешный запрос
- `400` - Неверные параметры запроса
- `404` - Ресурс не найден
- `500` - Внутренняя ошибка сервера

Формат ответа при ошибке:
```json
{
  "message": "Описание ошибки"
}
```

---

## 🔄 Задержка ответа

Для имитации реального API все запросы имеют задержку **800ms**.

---

## 📦 Зависимости

- `json-server` версии `^0.17.4`

---

## 📄 Лицензия

Внутренний проект для изучения языков.

